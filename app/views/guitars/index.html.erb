<div class="products-namespace">
	<h2 class="page-header text-center">Guitars</h2>
    <div id="paginator" class="text-center">
        <%= paginate @guitars, remote: true %>
    </div>
    <div id="products" class="row list-group">
            <%= render partial: 'products/product',
                       collection: @guitars,
                       as: :product %>
    </div>
    <br>
    <div id="paginator" class="text-center">
        <%= paginate @guitars, remote: true %>
    </div>
    <br>
</div>